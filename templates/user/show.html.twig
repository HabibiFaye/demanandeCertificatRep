{% extends 'base.html.twig' %} {% block title %}User{% endblock %} {% block body %}
<h1 class=" mt-3 text-center ">Etat de la demande</h1>

<div class=" container mt-5">
    <table class="table table-responsive table-bordered table-hover text-capitalize text-center">
        <tr class=" table-active table-dark text-uppercase text-white">
            <th>Nom</th>
            <th>Prenom</th>
            <th>Date demande(aa/mm/jj)</th>
            <th>etat</th>
            <th>Action</th>
        </tr>
        <tr>
            <td>{{ user.nom }}</td>
            <td>{{ user.prenom }}</td>
            <td>{{ user.createdAt ? user.createdAt|date('y/m/d ') : '' }}</td>
            <td class="table-active  table-success text-uppercase text-white">{{ user.demandes[0].statut }}</td>
            <td>
                <button class="btn btn-secondary "> <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="text-light text-decoration-none">modifier</a> </button>
                <button class="btn btn-secondary"> <a href="{{path('app_user_demandepdf')}}" class=" text-white text-decoration-none">impression</a> </button>
            </td>

        </tr>
    </table>

    <a class="text-dark" href="{{path('app_home')}}">Retour dans la page d'acceuil</a> {% endblock %}