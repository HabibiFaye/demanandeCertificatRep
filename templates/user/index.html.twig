{% extends 'base.html.twig' %} {% block title %}User index{% endblock %} {% block body %}
<h1 class=" mt-3 text-center ">Liste des Demandes</h1>
<div class=" container mt-5">
    <table class="table">
        <tr>
            <th>Nom</th>
            <th>Prenom</th>
            <th>Date demande(aa/mm/jj)</th>
            <th>etat</th>
            <th>Action</th>
        </tr>

        {% for user in users %}
        <tr>
            <td>{{ user.nom }}</td>
            <td>{{ user.prenom }}</td>
            <td>{{ user.createdAt ? user.createdAt|date('y/m/d H:i') : '' }}</td>
            <td class="table-active  table-success text-uppercase text-white">{{ user.demandes[0].statut }}</td>
            <td>
                <button class="btn btn-primary"> <a href="{{path('app_user_demandepdf')}}" class=" text-white text-decoration-none">Valider</a> </button>
                <button class="btn btn-secondary "> <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="text-light text-decoration-none">modifier</a> </button>
                <button class="btn btn-secondary"> <a href="{{path('app_user_demandepdf')}}" class=" text-white text-decoration-none">impression</a> </button>
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="4">no records found</td>
        </tr>
        {% endfor %}

    </table>

</div>


<a href="{{ path('app_user_new') }}">Create new</a> {% endblock %}